# Copy to `.env` and set a real admin password or password hash
# Option A (simple, development): set a plain password (NOT recommended for production)
ADMIN_PASSWORD=change-me

# Option B (recommended): set a bcrypt hash of the admin password
# You can generate a hash locally with: node -e "console.log(require('bcryptjs').hashSync(process.argv[1], 10))" your_password
ADMIN_PASSWORD_HASH=

# JWT secret used to sign admin tokens (set a strong random value in production)
JWT_SECRET=change-me

# Server port
PORT=3000
# Copy to .env and set a strong admin password
ADMIN_PASSWORD=change-me
PORT=3000
