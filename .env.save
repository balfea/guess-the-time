# add .env to .gitignore if it isn't already
echo ".env" >> .gitignorquitquite
# add .env to .gitignore if it isn't already
echo ".env" >> .gitignore# add .env to .gitignore if it isn't already
echo ".env" >> .gitignore# add .env to .gitignore if it isn't already
echo ".env" >> .gitignorecd /Users/balfea/Desktop/GuessTheTime
open .env        # macOS editor, or use `nano .env`
# set ADMIN_PASSWORD=your_strong_passwordd /Users/balfea/Desktop/GuessTheTime
open .env        # macOS editor, or use `nano .env`
# set ADMIN_PASSWORD=your_strong_passwordkillcd /Users/balfea/Desktop/GuessTheTime
grep -qxF ".env" .gitignore || echo ".env" >> .gitignoree# Copy to .env and set a strong admin password
ADMIN_PASSWORD=change-me
PORT=3000
